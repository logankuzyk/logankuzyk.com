# Base Docker Compose configuration
# This file defines shared networks and volumes used by dev and prod overrides

networks:
  app_network:
    driver: bridge

volumes:
  postgres_dev_data:
    driver: local
  postgres_prod_data:
    driver: local

